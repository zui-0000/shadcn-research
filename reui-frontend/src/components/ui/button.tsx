/**
 * ReUI の Button を `~/components/ui/button` から import するためのラッパー。
 * API は ReUI そのまま（`buttonStyle`, `color`, react-aria の Button 系 props）。
 * @see https://reui.dev
 */
export { Button } from "@reui/reui";
